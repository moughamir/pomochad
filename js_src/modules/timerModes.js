/* import { defaultMode, breakMode, automaticMode } from "./divSelectors";
import { totalTime } from "../index";

breakMode.addEventListener("click", () => {
  totalTime = 5;
});

defaultMode.addEventListener("click", () => {
  totalTime = 25;
});
automaticMode.addEventListener("click", () => {
  totalTime = 656565;
}); */
